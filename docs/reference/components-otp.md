# components/otp

<!-- GENERATED by scripts/build-docs.ts from components/otp/otp.ts — edit the source, then `deno task docs`. -->

Import: `import { Otp } from "@tundralibs/ui/otp"`

Rendered in every variant on the catalogue (`deno task build:demos`, then `demo/index.html`). Guides: [Getting started](../UI-Getting-Started.md) · [rAPId integration](../UI-Rapid.md) · [Theming](../UI-Theming.md).

## Functions

### `Otp`

```ts
Otp(props: OtpProps): Html
```

One-time code entry. Progressive: without JS a single
`autocomplete="one-time-code"` input is what the user sees and
submits; with JS (otp.js) it becomes the hidden value carrier behind
N cells that auto-advance, accept a pasted/autofilled code across all
cells, and walk back on Backspace. The form always submits exactly one
field, `name`, whichever way it was entered.

## Types

### `OtpProps`

| Prop | Type | Required | Description |
| --- | --- | --- | --- |
| `id` | `string` | yes | Required: cells are `<id>-1 … <id>-N`, the label targets the first. |
| `name` | `string` | yes | The submitted field — ONE value, the joined code. |
| `length` | `number` |  | Number of cells. Default 6. |
| `value` | `string` |  | Prefilled code (e.g. re-rendered after a failed attempt). |
| `label` | `string | Html` |  |  |
| `hint` | `string | Html` |  |  |
| `error` | `string` |  | Set from `RapidFormError.fields[name]` (§6). |
| `mode` | `"numeric" | "alphanumeric"` |  | `numeric` (default) uses the numeric keyboard and accepts digits only. |
| `groups` | `number` |  | Visual separator after every `groups` cells, e.g. 3 → `123 · 456`. |
| `autofocus` | `boolean` |  |  |
| `disabled` | `boolean` |  |  |
| `autoSubmit` | `boolean` |  | Submit the surrounding form as soon as every cell is filled. |
| `attrs` | `Attrs` |  |  |

## CSS hooks

Classes defined by `components/otp/otp.css` — structural, token-driven; override from an unlayered stylesheet (see [Theming](../UI-Theming.md)):

`.js`, `.otp`, `.otp--invalid`, `.otp__cell`, `.otp__cells`, `.otp__sep`, `.otp__value`

## Behaviour

`components/otp/otp.js` ships in `ui.js` (delegated on `document`, re-initialised after a rAPId swap).

Attributes it reads or writes: `data-filled`, `data-otp`, `data-otp-completed`, `data-otp-mode`, `data-otp-submit`.

Events: `otp:complete`, `rapid:swapped`.
